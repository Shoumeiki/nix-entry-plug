_: {
  # Installs the binary, registers a session entry in greetd, and enables polkit.
  # Per-user config (keybinds, monitors, hyprpaper, hyprlock) lives in home/desktop/.
  programs.hyprland.enable = true;
}
