# Encrypt this file with sops before committing.
# Values below are placeholders — replace all before encrypting.
users:
  ellen:
    password: "REPLACE_WITH_YESCRYPT_HASH"
  guest:
    password: "REPLACE_WITH_YESCRYPT_HASH"
ssh:
  ellen-authorized-keys: |
    ssh-ed25519 AAAAC3NzaC1lZDI1NTE5AAAAIREPLACE_ME ellen@host
seafile:
  db-root-password: "REPLACE_WITH_STRONG_PASSWORD"
  admin-email: "ellen@example.com"
  admin-password: "REPLACE_WITH_STRONG_PASSWORD"
